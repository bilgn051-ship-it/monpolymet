import GroupStructure from '../../components/ui/GroupStructure';

export default function CompaniesPage({ lang, t }) {
  return (
    <div className="sectors-page-container container-padding">
      <GroupStructure lang={lang} t={t} />
    </div>
  );
}
